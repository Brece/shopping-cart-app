import React from 'react';

const Product = (props) => {
    // FIXME: remove console.log
    // console.log(props);

    return (
        <section className='c-product'>
            product xxx
        </section>
    );
}

export default Product;
