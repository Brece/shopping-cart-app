import React from "react";
import Breadcrumb from "./Breadcrumb";

const Cart = (props) => {
    return (
        <section>
            <Breadcrumb />
            <div>Cart xxx</div>
        </section>
    );
}

export default Cart;
