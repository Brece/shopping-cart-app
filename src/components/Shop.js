import React from "react";
import Breadcrumb from "./Breadcrumb";

const Shop = (props) => {
    return (
        <section>
            <Breadcrumb />
            Shop xxx
        </section>
    );
}

export default Shop;
